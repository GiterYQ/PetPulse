# PetPulse 前端 lint 基线修复计划

## 目标

修复当前 React/Vite Demo 的 lint 问题，让前端基线更适合后续接入 API，同时不做 UI 重写、不引入新依赖、不改变产品流程。

## 阶段

1. 复现 lint 失败并记录错误：完成
2. 定位触发 lint 的代码和根因：完成
3. 做最小代码修改：完成
4. 运行 lint/build 验证：完成
5. 更新规划记录和交付说明：完成

## 约束

- 只修 lint 相关问题。
- 保持当前演示 UI 和交互不变。
- 代码注释如需新增，用中文。
- 不回滚用户或其它 Agent 的无关改动。
