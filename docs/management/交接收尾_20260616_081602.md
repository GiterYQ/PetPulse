# PetPulse 交接收尾记录

> 时间戳：2026-06-16 08:16:02 CST
> 用途：给下一个对话或下一位 Agent 快速接手，避免重复理解项目。

## 1. 当前状态

当前仓库仍是 React/Vite 展会 Demo，不是生产级 App，也没有真实后端。

本轮主要完成了中文项目规则、MVP 产品草案、工程规则、API 草案、OpenAPI 契约和实施路线图。MVP 暂时不绑定 Flutter / React Native / 原生，先走 API-first 路线。

核心口径：

```text
账号 -> 宠物档案 -> 发起求助 -> 附近匹配 -> 发送提醒 -> 对方响应 -> 历史/信任沉淀
```

## 2. 已完成内容

### 2.1 项目规则和协作规则

- 新增 `AGENTS.md`。
- 新增 `docs/project-rules.md`。
- 新增 `docs/engineering/AI协作规则与提示词.md`。
- 明确中文沟通、中文文档、必要中文注释规则。
- 明确当前 Demo 不是生产 App。

### 2.2 文档组织

- 新增 `docs/README.md`。
- 新增 `docs/management/文档组织结构说明.md`。
- 新增 `docs/management/Markdown文档状态清单_20260615.md`。
- 新增本文件：`docs/management/交接收尾_20260616_081602.md`。

### 2.3 产品和 MVP

- 新增 `docs/product/MVP需求草案.md`。
- 新增 `docs/product/MVP功能分层与隐藏功能清单.md`。
- 新增 `docs/product/用户数据与云端同步预留方案.md`。
- 新增 `docs/product/合规与安全文案规则.md`。
- 明确 MVP 不做支付、保险、复杂信用分、AI 医疗诊断和公开精确地址。

### 2.4 工程与架构

- 新增 `docs/engineering/本地化与区域化准备规则.md`。
- 新增 `docs/engineering/功能开关与Provider规则.md`。
- 新增 `docs/engineering/API与数据模型草案.md`。
- 新增 `docs/engineering/MVP技术实施路线图.md`。
- 推荐 API-first + 模块化单体。
- 保留未来 Flutter、React Native、Web、原生客户端选择空间。

### 2.5 API 契约

- 新增 `docs/api/openapi.yaml`。
- OpenAPI 覆盖：
  - Config
  - Auth
  - Devices
  - Pets
  - Emergencies
  - Matching
  - Alerts
  - Responses
  - Media
  - Admin
- 契约使用 `openapi: 3.1.0`。
- 共 33 个 `operationId`。

### 2.6 QA、发布和提示词

- 新增 `docs/qa/MVP发布前最小手测脚本.md`。
- 新增 `docs/release/MVP上线前检查清单.md`。
- 新增 `docs/prompts/petpulse-prompt-pack.md`。

### 2.7 前端 lint 基线

已在本地修复两个 lint 问题：

- `src/components/EmergencyMap.tsx`
  - 将 `useRef<number>(Date.now())` 改为 `useRef<number>(0)`。
  - 避免 React render 阶段调用 impure function。
- `src/components/AIProcessingFeed.tsx`
  - 补齐 `useEffect` 依赖：`active, steps, onPhaseChange, onComplete`。

验证：

- `npm run lint` 通过。
- `npm run build` 通过。

注意：当前 `.gitignore` 明确忽略 `src/`、`package.json`、`public/`、配置文件和构建产物，并标注 `Source code (kept local only)`。因此这两个源码修复默认不会被普通 `git add` 提交。

## 3. 已做验证

### 3.1 前端

```bash
npm run lint
npm run build
```

结果：均通过。

### 3.2 OpenAPI

已验证：

- YAML 可解析。
- 84 个内部 `$ref` 可解析。
- 33 个 `operationId` 无重复。
- 未发现 `TODO/TBD/FIXME`、乱码或禁用风险文案。

## 4. 当前 Git 注意事项

当前 `.gitignore` 会忽略：

- `src/`
- `public/`
- `index.html`
- `vite.config.ts`
- `tsconfig*.json`
- `eslint.config.js`
- `package.json`
- `package-lock.json`
- `dist/`

这意味着：

- 文档和规划文件可以正常提交。
- 前端源码修复仍留在本地工作区，但不会进入普通提交。
- 如果后续决定把源码也纳入版本控制，应先修改 `.gitignore` 并单独审查，不要用 `git add .`。

不要提交：

- `.claude-design/`
- `20260411_PetPulse_yeqian.pptx`
- `dist/`
- `node_modules/`
- `.DS_Store`

## 5. 剩余未做任务

### 5.1 高优先级

- [ ] 设计数据库 schema 草案。
  - 建议文件：`docs/engineering/数据库Schema草案.md`
  - 覆盖 `User`、`Pet`、`Emergency`、`MatchCandidate`、`AlertDelivery`、`Response`、`MediaAsset`、`AdminAction`。
  - 同步考虑索引、软删除、位置隐私和状态流转。
- [ ] 抽取前端状态枚举和 mock 数据边界。
  - 当前 Demo 仍是 mock 驱动。
  - 后续应把 mock 数据和真实 API client stub 分层。
- [ ] 设计 `apiClient` 占位层。
  - 对齐 `docs/api/openapi.yaml`。
  - API base URL 走环境变量，不写死。

### 5.2 中优先级

- [ ] 决定后端技术栈。
  - 当前建议：API-only 模块化单体。
  - 数据库建议：PostgreSQL，地理能力预留。
- [ ] 创建 `services/api` 后端骨架。
  - health check。
  - ready check。
  - 统一错误格式。
  - requestId。
  - 配置校验。
  - 输入校验。
- [ ] 实现 `User`、`Pet`、`Emergency` 最小 CRUD。
- [ ] 实现 mock matching、mock alert、response。
- [ ] 设计最小后台页面或临时后台方案。

### 5.3 后置任务

- [ ] Flutter / React Native / 原生移动端最终选型。
  - 建议等后端 API 和核心闭环稳定后再决定。
- [ ] 真实 Push Provider。
- [ ] 真实地图 Provider。
- [ ] 真实对象存储 Provider。
- [ ] AI 文案草稿能力。
  - 只做信息整理和草稿，不做医疗诊断。
- [ ] 完整多语言发布。
- [ ] 多地区合规后台。

## 6. 下个对话建议起手提示

可以直接说：

```text
继续 PetPulse。先读 AGENTS.md、docs/README.md、docs/management/交接收尾_20260616_081602.md。
下一步请做 docs/engineering/数据库Schema草案.md，并同步更新 docs/README.md 和 Markdown 文档状态清单。
保持中文文档和中文注释规则，不要提交 .claude-design、PPT、dist 或 node_modules。
```

## 7. 当前推荐下一步

下一步最适合做：

```text
新增 docs/engineering/数据库Schema草案.md
```

原因：

- `docs/api/openapi.yaml` 已经有接口和对象。
- 数据库 schema 是创建后端骨架前必须明确的边界。
- 先写 schema 可以提前处理位置隐私、软删除、索引和状态流转问题。
